# ────────────────────────────────────────────────────────────────
# RPPlatform 환경 변수
# 이 파일은 .gitignore에 포함되어야 합니다. 절대 커밋하지 마세요.
# EAS Build에서는 eas.json의 env 섹션 또는 EAS Secrets를 사용하세요.
# ────────────────────────────────────────────────────────────────

# Google AdMob 앱 ID
ADMOB_APP_ID=ca-app-pub-9020691040370881~6462881546

# Hugging Face 토큰 → 앱에서 제거, 서버 secret으로 이동
# wrangler secret put HF_TOKEN 으로 등록됨

# Amplitude / Sentry → 앱 번들에서 제거됨 (보안)
# 아래 명령으로 Worker secret에 등록하세요:
#   wrangler secret put AMPLITUDE_API_KEY
#   wrangler secret put SENTRY_DSN
# 앱은 시작 시 /api/client-config 에서 자동으로 가져옵니다.

# EAS 프로젝트 ID
